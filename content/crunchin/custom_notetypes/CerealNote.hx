function setupNote(note){
    note.reloadNote('notes/cereal-');
    note.colorSwap.hue = 0;
    note.colorSwap.saturation = 0;
    note.colorSwap.brightness = 0;
    note.canMiss = true;
    note.offsetX += 7;
}